# Funnel-Recon
authorized URL reconnaissance by 'SydneySpider'.  A bounded, rate-limited Go CLI that enumerates subdomains, probes live hosts, crawls pages, looks up historical URLs, inspects scoped JavaScript, and reports unverified parameter heuristics. It never sends exploitation payloads, submits forms, or prints extracted credential values.
